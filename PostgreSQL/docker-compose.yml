version: '3.1'

services:

  db:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      # User: postgres
      POSTGRES_PASSWORD: bloodyroots
